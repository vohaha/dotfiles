---
- name: Find hook scripts
  ansible.builtin.find:
    paths: "{{ playbook_dir }}/scripts/hooks"
    file_type: file
  register: hook_files

- name: Install git hooks
  ansible.builtin.copy:
    src: "{{ item.path }}"
    dest: "{{ playbook_dir }}/.git/hooks/{{ item.path | basename }}"
    mode: "0755"
  loop: "{{ hook_files.files }}"
