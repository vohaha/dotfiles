# Domain Knowledge

Detailed domain context for Claude. Files here are read before designing changes —
they provide the expertise a new session needs.

Keep files focused: one concept per file, written for an agent that has never seen this domain before.

Examples:
- `layer-model.md` — the mental model of system layers and which tools cover each
- `cross-platform-patterns.md` — how OS-specific config is handled in this repo
